services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
  dashboard:
    build: .
    command: streamlit run src/dashboard/app.py --server.address 0.0.0.0
    ports:
      - "8501:8501"
    depends_on:
      - api
